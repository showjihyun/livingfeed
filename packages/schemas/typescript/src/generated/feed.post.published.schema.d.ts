/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * FeedComposer의 편집 결정 — feed-worthiness 임계를 통과한 사건이 렌더링 가능한 FeedItem으로 승격되었다 (ADR-014 §1단). 봉투의 event_id가 곧 post id이자 피드 커서(ULID)다 (ADR-010 §SSE). 원본 사건은 봉투 causation_id로, 서사 사슬은 correlation_id로 추적한다.
 */
export interface FeedPostPublished {
  /**
   * 가시성 등급 — 6가지 피드는 등급이 다른 같은 데이터다 (ADR-014, 파이프라인 복제 금지)
   */
  visibility: "world" | "community" | "relationship" | "personal" | "private" | "hidden";
  /**
   * 피드 카드 제목 (템플릿 생성)
   */
  title: string;
  /**
   * 서사 본문 — 템플릿 우선, drama 상위 아이템만 LLM 내레이션 (ADR-014 비용 게이트)
   */
  body: string;
  /**
   * 본문 생성 방식 — 비용 게이트 감사용
   */
  narration_kind: "template" | "llm";
  /**
   * 참여 액터 전원 (작성 주체는 봉투 actor_id)
   *
   * @minItems 1
   */
  participants: [string, ...string[]];
  /**
   * Community 가시성일 때의 대상 커뮤니티
   */
  community_id: string | null;
  location_id: string | null;
  /**
   * 사건의 드라마 점수 0..1 — 읽기 랭킹의 최대 가중 항 (ADR-014 §2단)
   */
  drama_score: number;
  /**
   * 편집 점수 = f(drama, 관계 근접도, 희소성, Director boost) — 임계 통과값 (ADR-014 §1단)
   */
  worthiness: number;
  /**
   * 승격된 원본 사건의 타입 (원본 event_id는 봉투 causation_id)
   */
  source_event_type: string;
  /**
   * 필터·검색용 태그 (action_kind 등)
   */
  tags: string[];
  /**
   * 미디어 슬롯 (ADR-014) — MVP는 빈 배열
   */
  media: {
    [k: string]: unknown;
  }[];
}
