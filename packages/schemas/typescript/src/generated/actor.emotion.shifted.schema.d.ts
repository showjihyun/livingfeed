/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * 액터 감정 상태의 유의미한 변화 (ADR-015). 변화가 임계 이상일 때만 발행된다 — mood는 PAD 3차원, emotions는 대상 있는 활성 감정 인스턴스 상위 목록. 원인은 봉투 causation_id가 가리킨다.
 */
export interface ActorEmotionShifted {
  /**
   * PAD 무드 벡터 — 느리게 변하고 baseline(성격)으로 회귀한다
   */
  mood: {
    pleasure: number;
    arousal: number;
    dominance: number;
  };
  /**
   * 활성 감정 인스턴스 상위(강도순) — '화남'이 아니라 '누구 때문에 화남'
   *
   * @maxItems 8
   */
  emotions:
    | []
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ]
    | [
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        },
        {
          type: string;
          intensity: number;
          /**
           * 감정의 대상 (액터 또는 플레이어, 없으면 null)
           */
          target_id: string | null;
        }
      ];
  /**
   * 사람이 읽는 한 줄 원인 요약 (디버깅·내레이션 입력)
   */
  reason: string;
}
