{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://livingfeed.dev/schemas/events/actor.message.sent.schema.json",
  "title": "ActorMessageSent",
  "description": "액터가 플레이어에게 보낸 메시지 — 댓글 답글 또는 DM (ADR-012 상호작용 경로). 봉투 causation_id가 원인이 된 player.* 이벤트를 가리키고, gateway가 이 이벤트를 WS 세션으로 push한다 (ADR-010).",
  "type": "object",
  "properties": {
    "channel": {
      "type": "string",
      "enum": ["comment", "dm"]
    },
    "target_player_id": {
      "type": "string",
      "pattern": "^p_[a-z0-9_]+$"
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1000
    },
    "post_id": {
      "type": ["string", "null"],
      "description": "comment 채널일 때 대상 피드 포스트 (dm이면 null)"
    },
    "in_reply_to": {
      "type": "string",
      "description": "응답 대상 player.* 이벤트의 event_id (봉투 causation_id와 동일 — 클라이언트 편의 중복)",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"
    }
  },
  "required": ["channel", "target_player_id", "text", "post_id", "in_reply_to"],
  "additionalProperties": false
}
