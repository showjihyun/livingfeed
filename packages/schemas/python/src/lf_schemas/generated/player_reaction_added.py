# generated by datamodel-codegen:
#   filename:  player.reaction.added.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field, constr


class Kind(StrEnum):
    like = "like"


class PlayerReactionAdded(BaseModel):
    """
    플레이어가 피드 포스트에 반응(좋아요)을 남겼다 (ADR-010 §WS 세션). 응답 의무는 없다 — 액터의 지각·감정 입력이 된다 (ADR-012/015: '좋아요 하나로도 세계는 당신을 알아차린다').
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    player_id: constr(pattern=r"^p_[a-z0-9_]+$")
    target_actor_id: str = Field(..., description="포스트 작성 액터")
    post_id: constr(pattern=r"^[0-9A-HJKMNP-TV-Z]{26}$") = Field(
        ..., description="반응이 달린 피드 포스트 (feed.post.published event_id)"
    )
    kind: Kind
