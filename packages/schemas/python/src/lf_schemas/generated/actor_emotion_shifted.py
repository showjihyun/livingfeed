# generated by datamodel-codegen:
#   filename:  actor.emotion.shifted.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, confloat, constr


class Mood(BaseModel):
    """
    PAD 무드 벡터 — 느리게 변하고 baseline(성격)으로 회귀한다
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    pleasure: confloat(ge=-1.0, le=1.0)
    arousal: confloat(ge=-1.0, le=1.0)
    dominance: confloat(ge=-1.0, le=1.0)


class Emotion(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    type: constr(pattern=r"^[a-z_]+$")
    intensity: confloat(ge=0.0, le=1.0)
    target_id: str | None = Field(
        ..., description="감정의 대상 (액터 또는 플레이어, 없으면 null)"
    )


class ActorEmotionShifted(BaseModel):
    """
    액터 감정 상태의 유의미한 변화 (ADR-015). 변화가 임계 이상일 때만 발행된다 — mood는 PAD 3차원, emotions는 대상 있는 활성 감정 인스턴스 상위 목록. 원인은 봉투 causation_id가 가리킨다.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    mood: Mood = Field(
        ..., description="PAD 무드 벡터 — 느리게 변하고 baseline(성격)으로 회귀한다"
    )
    emotions: list[Emotion] = Field(
        ...,
        description="활성 감정 인스턴스 상위(강도순) — '화남'이 아니라 '누구 때문에 화남'",
        max_length=8,
    )
    reason: constr(max_length=200) = Field(
        ..., description="사람이 읽는 한 줄 원인 요약 (디버깅·내레이션 입력)"
    )
