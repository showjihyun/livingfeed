# generated by datamodel-codegen:
#   filename:  relationship.milestone.reached.schema.json

from __future__ import annotations

from enum import StrEnum

from pydantic import BaseModel, ConfigDict, Field, constr


class Stage(StrEnum):
    """
    마일스톤 이후의 stage
    """

    stranger = "stranger"
    acquaintance = "acquaintance"
    friend = "friend"
    close_friend = "close_friend"
    romantic = "romantic"
    family = "family"
    mentor = "mentor"
    rival = "rival"
    enemy = "enemy"


class RelationshipMilestoneReached(BaseModel):
    """
    관계의 마일스톤 — 첫 만남, stage 전이, 배신 등 (ADR-016 §이력과 마일스톤). 에피소드 기억에 고중요도로 응고되고(ADR-008) Context Fabric의 관계 요약 재료가 된다. stage 전이는 수치가 아니라 이벤트가 만든다.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    from_id: str
    to_id: str
    milestone: constr(pattern=r"^[a-z_]+$") = Field(
        ..., description="마일스톤 종류 (first_met, stage_transition, betrayal, ...)"
    )
    stage: Stage = Field(..., description="마일스톤 이후의 stage")
    note: constr(max_length=200) = Field(
        ..., description="관계 요약 문장의 재료 — '어쩌다 이렇게 됐는지'의 한 줄"
    )
