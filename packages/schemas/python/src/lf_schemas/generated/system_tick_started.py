# generated by datamodel-codegen:
#   filename:  system.tick.started.schema.json

from __future__ import annotations

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint


class Scheduled(BaseModel):
    """
    티어별 DECIDE 예정 수 (ADR-011 LOD)
    """

    hot: conint(ge=0)
    warm: conint(ge=0)
    cold: conint(ge=0)


class SystemTickStarted(BaseModel):
    """
    tick 파이프라인 시작 신호 (ADR-011). scheduled는 이번 tick의 DECIDE 대상 수.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    tick: conint(ge=0)
    started_at: AwareDatetime
    scheduled: Scheduled = Field(..., description="티어별 DECIDE 예정 수 (ADR-011 LOD)")
