# generated by datamodel-codegen:
#   filename:  world.incident.occurred.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, confloat, constr


class WorldIncidentOccurred(BaseModel):
    """
    환경 사건 — Director의 간접 개입 도구 inject_incident의 출력 (ADR-013). 액터를 조종하지 않는다: 사건은 환경에 놓이고, 반응은 액터의 몫이다. FeedComposer의 승격 대상 (ADR-014 '세계 뉴스').
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    incident_kind: constr(pattern=r"^[a-z_]+$") = Field(
        ..., description="사건 분류 (blackout, rumor_spread, chance_encounter, ...)"
    )
    description: constr(min_length=1, max_length=300) = Field(
        ..., description="사건 서술 한 줄 — 피드 내레이션과 액터 지각의 입력"
    )
    location_id: str | None
    affected_actor_ids: list[str] = Field(
        ..., description="사건의 영향권 액터 (지각 대상 — 강제 반응 아님)"
    )
    intensity: confloat(ge=0.0, le=1.0) = Field(
        ..., description="사건 강도 0..1 — 피드 drama 점수와 지각 우선순위의 입력"
    )
