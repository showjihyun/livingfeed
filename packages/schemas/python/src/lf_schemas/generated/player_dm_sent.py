# generated by datamodel-codegen:
#   filename:  player.dm.sent.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class PlayerDmSent(BaseModel):
    """
    플레이어가 액터에게 1:1 DM을 보냈다 (ADR-010 §WS 세션). gateway가 세션 커맨드를 검증해 적재한다 — 개입은 흔적을 남긴다 (docs/plan/02). 액터의 응답은 actor.message.sent로, causation_id가 이 이벤트를 가리킨다.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    player_id: constr(pattern=r"^p_[a-z0-9_]+$")
    target_actor_id: str = Field(
        ..., description="DM 수신 액터 — 메일박스 라우팅 키 (ADR-012)"
    )
    text: constr(min_length=1, max_length=1000)
