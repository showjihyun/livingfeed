# generated by datamodel-codegen:
#   filename:  actor.identity.declared.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, confloat, constr


class Goal(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    description: constr(min_length=1, max_length=200)
    priority: confloat(ge=0.0, le=1.0)


class ActorIdentityDeclared(BaseModel):
    """
    액터가 세계에 등장하며 정체성을 선언한다 — 페르소나 정체성(agents/personas, ADR-012)이 read 모델로 노출되는 경로. 정체성은 불변이라 세계당 1회 발행되고, pg-projector가 read.actors로 프로젝션해 웹 클라이언트가 실제 이름·소개·목표를 읽는다 (표시 데이터 하드코딩 금지).
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    name: constr(min_length=1, max_length=80) = Field(
        ..., description="표시 이름 (프로필·DM·피드 헤더)"
    )
    archetype: constr(pattern=r"^[a-z_]+$") = Field(
        ..., description="원형 분류 — 검색·표시 태그"
    )
    bio: constr(min_length=1, max_length=500) = Field(
        ..., description="identity_core 요약 — 프로필 소개문"
    )
    goals: list[Goal] = Field(
        ..., description="공개 목표 (프로필 표시) — 우선순위순은 소비자 몫"
    )
