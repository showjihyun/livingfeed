# generated by datamodel-codegen:
#   filename:  actor.goal.achieved.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class ActorGoalAchieved(BaseModel):
    """
    액터가 목표를 이뤘다 (progress가 1.0에 도달, ADR-012/014). actor.goal.advanced가 한 걸음이라면 이것은 서사의 마디다 — FeedComposer가 '세계 뉴스'로 승격하고 강한 기쁨을 낳는다. 세계당 목표당 1회.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    goal_id: constr(pattern=r"^[a-z0-9_]+$") = Field(
        ..., description="이룬 목표의 식별자 (agents/personas goals[].id)"
    )
    description: constr(min_length=1, max_length=200) = Field(
        ..., description="목표 서술 — 피드 내레이션에 실린다"
    )
    need: constr(pattern=r"^[a-z_]+$") = Field(
        ..., description="이 목표가 걸린 욕구 축 (achievement, belonging, security)"
    )
