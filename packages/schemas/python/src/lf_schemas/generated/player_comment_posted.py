# generated by datamodel-codegen:
#   filename:  player.comment.posted.schema.json

from __future__ import annotations

from pydantic import BaseModel, ConfigDict, Field, constr


class PlayerCommentPosted(BaseModel):
    """
    플레이어가 피드 포스트에 댓글을 남겼다 (ADR-010 §WS 세션). post_id는 feed.post.published의 event_id(ULID)다. 대상 액터의 응답은 actor.message.sent(channel=comment)로 돌아온다.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    player_id: constr(pattern=r"^p_[a-z0-9_]+$")
    target_actor_id: str = Field(
        ..., description="포스트 작성 액터 — 메일박스 라우팅 키 (ADR-012)"
    )
    post_id: constr(pattern=r"^[0-9A-HJKMNP-TV-Z]{26}$") = Field(
        ..., description="댓글이 달린 피드 포스트 (feed.post.published event_id)"
    )
    text: constr(min_length=1, max_length=500)
