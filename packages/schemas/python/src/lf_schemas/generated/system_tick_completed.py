# generated by datamodel-codegen:
#   filename:  system.tick.completed.schema.json

from __future__ import annotations

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint


class ActorsDecided(BaseModel):
    """
    티어별 DECIDE 수행 수 (ADR-011 LOD)
    """

    hot: conint(ge=0)
    warm: conint(ge=0)
    cold: conint(ge=0)


class SystemTickCompleted(BaseModel):
    """
    tick 파이프라인 완주 신호 (ADR-011). 프로젝터/엔진의 동기화 경계.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    tick: conint(ge=0)
    started_at: AwareDatetime
    completed_at: AwareDatetime
    duration_ms: conint(ge=0) = Field(..., description="예산: < 60000ms (ADR-020 §1)")
    actors_decided: ActorsDecided = Field(
        ..., description="티어별 DECIDE 수행 수 (ADR-011 LOD)"
    )
    events_emitted: conint(ge=0)
