[project]
name = "lf-relationship"
version = "0.1.0"
description = "Relationship Engine — 방향성 5차원 관계 + stage (ADR-016)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lf_relationship"]
