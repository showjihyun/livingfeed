[project]
name = "lf-emotion"
version = "0.1.0"
description = "Emotion Engine — PAD mood + OCC appraisal, 규칙 기반 (ADR-015)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lf_emotion"]
