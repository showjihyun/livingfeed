# Node
node_modules/
.next/
dist/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# 내부 문서 — 원격 저장소에 올리지 않는다 (ADR/기획/디자인 핸드오프)
/docs/

# Local data (compose volumes 등)
data/
*.local

# 호스트 기동 스크립트의 서비스 로그 (run-backend.*, run-shard-workers.*)
logs/

# Playwright MCP 스모크 산출물 (로컬 전용)
.playwright-mcp/

# Claude Code 세션 산출물 (에이전트·스킬·worktree) — 로컬 전용
.claude/

# 개발용 스크린샷 (저장소 루트) — QA 캡처는 로컬 전용, 커밋하지 않는다.
# 루트만 겨눈다(/*.png) — 하위의 정식 이미지 에셋(예: apps/web/public)은 살린다.
/*.png
